# Stacks Wallet Connection Labs

Portal React para interagir com contratos Clarity na rede Stacks, desenvolvido para o desafio "Build on Stacks with WalletConnect".

## 🚀 Funcionalidades

### Tip Jar / Creator Tips
- Conecte sua carteira usando WalletConnect
- Envie tips em STX para criadores
- Visualize histórico on-chain de tips recebidos
- Veja ranking de top tippers
- Cada tip gera uma transação (fees)

## 📁 Estrutura do Projeto

```
.
├── clarity/          # Contratos Clarity
│   ├── contracts/
│   │   └── tip-jar.clar
│   └── tests/
│       └── tip-jar.test.ts
└── frontend/         # Portal React
    └── src/
        ├── components/
        ├── pages/
        └── utils/
```

## 🛠️ Tecnologias

### Clarity
- Contrato `tip-jar` com funções:
  - `tip(to, amount, memo)`: Envia tip
  - `get-tip-received(user, tip-id)`: Busca tip recebido
  - `get-tip-sent(sender, tip-id)`: Busca tip enviado
  - `get-tipper-stats(tipper)`: Estatísticas do tipper
  - `get-recipient-stats(recipient)`: Estatísticas do recipient

### Frontend
- React 19 + TypeScript
- Vite
- @stacks/connect-react (WalletConnect)
- Tailwind CSS
- React Router

## 🚀 Como Executar

### Clarity

```bash
cd clarity
npm install
npm test
```

### Frontend

```bash
cd frontend
npm install
npm run dev
```

## 📝 Notas

- O contrato está configurado para testnet por padrão
- Atualize `frontend/src/utils/contract.ts` com o endereço do contrato deployado
- Para produção, altere `StacksTestnet` para `StacksMainnet` nos componentes

## 🎯 Desafio Stacks

Este projeto atende aos requisitos:
- ✅ Build on Stacks with WalletConnect
- ✅ Use of WalletKit SDK ou AppKit
- ✅ Users and fees generated by smart contracts

